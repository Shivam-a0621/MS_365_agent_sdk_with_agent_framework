---
name: git-identity
description: Git author identity to use for commits in the agent-fastapi repo.
metadata:
  type: project
---

Commit to this repo as **Shivam-a0621** / **bhardwajshivam.2108@gmail.com** (set locally, not global).
The remote `origin` is https://github.com/Shivam-a0621/MS_365_agent_sdk_with_agent_framework.git and history commits straight to `main`.

**Why:** the working machine has no git user identity configured, so commits fail until set; the chat `userEmail` (uday.banka@automationedge.com) is NOT the right committer for this repo.
**How to apply:** before the first commit, run `git config user.email bhardwajshivam.2108@gmail.com` and `git config user.name Shivam-a0621` in the repo; push directly to `main`.
